// No seed data. Every domain — roster, users, cases, chat, sos, reports,
// events, notifications, logs — is real and lives over the CSOT, not seed.
// The only thing left here is a fallback self id used before a real
// bridge-minted identity arrives.
export const SELF_ID = 'R-ECHO-1';
